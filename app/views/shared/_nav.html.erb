<nav>
  <%= link_to 'home', root_path, class: 'btn btn-primary' %>
  <%= link_to 'Video', videos_path, class: 'btn btn-primary' %>
  <%= link_to 'Users', users_path, class: 'btn btn-primary' %>
  <p> </p>
  <%  if user_signed_in? %>

    <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-primary' %>  <p> </p>
    <p> </p>

    <%= link_to 'Profile', user_path(current_user), class: 'btn btn-primary' %>

  <% else %>
    <%= button_to 'Login', new_user_session_path, class: 'btn btn-primary' %>
    <p> </p>
    <%= button_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary' %>
  <% end %>
</nav>
